# определяет базовый образ, на основе которого создаётся локальный образ
FROM ubuntu

# определяет команды, выполняемые в командной оболочки внутри образа (при создании контейнера)
    #  -y  -  подавляет запрос на согласие установки программы, это ОБЯЗАТЕЛЬНО НЕОБХОДИМО !!!
RUN apt-get update && apt-get upgrade && apt install -y openjdk-11-jdk

####################################################################################################