On local arm:

Virtual machine -> Настройки -> Базовые -> Общие -> Дополнительно ->
Общий буфер обмена -> Двунаправленный

-------------------------------------------------------------------------------------

On virtual arm:

Устройства -> Подключить образ дополнительной гостевой ОС

Открыть Vbox_GAs -> запустить терминал из открывшейся папки

Запустить файл (./autorun.sh) -> по завершению - shutdown -r now
(Завершения не будет - нужно дождаться отсутствия появления дальнейших строк)

Если не получилось:
(sudo apt install bzip2)
(sudo apt-get install build-essential gcc make perl dkms) ->
Запустить файл (./autorun.sh) -> shutdown -r now

