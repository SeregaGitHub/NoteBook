
  Со временем сообщество Java-программистов изменило подход к логированию. Так появился единый API,
делающий ведение журнала независимым от конкретного фреймворка. У него нет работающей реализации,
он только предоставляет интерфейс для логирования.

  Такое решение устраняет проблему настройки разных фреймворков в одном проекте.
Вместо того чтобы привязываться к конкретной реализации логирования, они используют единый API.
Программист просто выбирает конкретный фреймворк, и все логи идут через него.

  В качестве единого API мы будем использовать фреймворк The Simple Logging Facade for Java (англ. «Простой фасад логирования для Java»),
или сокращённо SLF4J. А в качестве конкретной реализации — фреймворк Logback.
  В Java есть и стандартные классы и интерфейсы для записи событий в лог. Они находятся в пакете java.util.logging.
Но именно связка SLF4J и Logback обладает рядом преимуществ.
У неё высокая производительность — что очень важно, потому что обычно в логи записываются большие объёмы информации.
А ещё SLF4J предоставляет более удобные методы записи и, как уже описывалось выше,
позволяет работать с разными фреймворками логирования через адаптер. Logback, в свою очередь, обеспечивает более гибкую и функциональную настройку.
Можно, например, настроить автоматическое удаление файлов с логами, сжатие их в архивы,
создание отдельного файла с записями для каждого пользователя и многое другое.

  Ещё одна особенность этой связки в том, что SLF4J и Logback были разработаны одним автором.
Для их совместного использования не требуется адаптер, так как Logback полностью реализует API, предоставляемый фреймворком SLF4J.

💡 В ноябре 2021 года в одном из старейших фреймворков логирования — Log4j была обнаружена опасная уязвимость.
Поэтому в тех проектах, где использовалась связка SLF4J + Log4j, нужно было срочно заменить Log4j на другой фреймворк.
Программистам удалось сделать это довольно быстро — благодаря тому, что Log4j работала через API, предоставленный SLF4J,
и его замена на другой фреймворк не требовала вносить изменения в код самих проектов.

                                            Компоненты фреймворка логирования

Большинство известных фреймворков логирования в Java состоят из трёх компонентов:

-  Logger (от англ. log, «журнал») — специальный класс, который преобразует событие в объект и передаёт его дальше другим компонентам фреймворка.
-  Appender  (от англ. append, «добавлять») — интерфейс, который отвечает за добавление объекта, полученного от логера, непосредственно в лог.
   В зависимости от того, где находится журнал (в файле, в базе данных или, например, он нигде не хранится, а просто выводится на консоль), —
   используются разные реализации Appender: ConsoleAppender, FileAppender и так далее. При этом запись всегда добавляется в конец лога.
-  Layout (англ. «оформление», «формат») — интерфейс, который перед выводом записи из лога в консоль или на печать приводит её к определённому формату.
   От этого компонента зависит, будет ли в записи отображаться информация, например, о времени события или любые другие подробности.

В фреймворке Logback Logger — уже готовый класс, а для Appender и Layout есть несколько типовых реализаций, которых хватает для решения большинства задач.
Но при желании разработчик может написать свои.


                                                      Logger

  Чтобы добавить запись в лог, необходимо получить экземпляр типа org.slf4j.Logger. Его не нужно создавать самостоятельно,
и не важно, какая конкретная система логирования выбрана в связке с SLF4J. Обо всём позаботится специальный класс-фабрика LoggerFactory
и его статический метод getLogger(String).
При объявлении логера принято использовать модификаторы private, static и final. Логер одинаков для всех экземпляров класса,
поэтому можно сделать переменную статической, то есть создать один логер на класс. Также нет смысла менять экземпляр логера во время работы программы,
поэтому переменную помечают final. Ну и чтобы логером класса нельзя было воспользоваться вне этого класса, переменная скрыта модификатором private.

💡 Имена констант в Java, как вы уже знаете, принято писать заглавными буквами — в стиле SCREAMING_SNAKE_CASE (англ. «регистр кричащей змеи»).
Каждая константа объявляется как static final, но из этого не следует, что все поля с модификаторами static final являются константами.
Например, константами не являются объекты, которые объявлены для того, чтобы использовать их полезные методы в классе, а не само значение объекта.
К таким исключениям относится объект логера. Его имя принято записывать в lowerCamelCase (англ. «нижнийВерблюжийРегистр»), как для обычной переменной.
В проектах чаще всего можно встретить два варианта имени для этого поля: log и logger.
Итак, вызываем статический метод getLogger(String) и в качестве аргумента передаём ему желаемое имя логера:

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class Practicum {
    private static final Logger log = LoggerFactory.getLogger("Practicum");
    ...
}

  Кстати, имена логеров регистрозависимы. Это значит, что “Practicum”, “practicum” и “PracTicuM” будут считаться разными названиями.
В качестве имён для логеров принято использовать полное название класса, включающее название пакетов, в которые этот класс входит.
Во-первых, имя класса обычно отражает его функционал.
Во-вторых, имя логера отображается в логах, поэтому сразу понятно, из какого класса отправлена запись.
Для удобства у LoggerFactory есть ещё одна версия метода getLogger, принимающая параметр типа Class<?>,
из которого впоследствии фабрика получает полное имя класса (включающее пакеты):

package ru.practicum.logging;  // указываем пакет, в который входит класс Practicum

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

class Practicum {
    // получаем логер с именем класса, к которому он относится
    private final static Logger log = LoggerFactory.getLogger(Practicum.class);

    public static void main(String[] args) {
        // выводим имя логгера
        System.out.println(log.getName()); // будет выведено: ru.practicum.logging.Practicum
    }
}

  В этом коде мы сначала получили логер с именем класса. Потом, в main, мы вызвали метод getName(), чтобы проверить,
что фабрика создала логер с нужным именем ru.practicum.logging.Practicum.

                                                          Appenders

  Классы, реализующие интерфейс Appender, передают информацию, полученную от логера, в лог. Так как записи упорядочены по дате и времени добавления,
то каждая новая всегда добавляется в конец журнала.
В составе Logback есть большой набор разных реализаций типа Appender. Например, для сохранения логов в базу данных, передачи их по сети,
отправки по почте и так далее. Но всё же наиболее часто используются следующие:

-  ConsoleAppender (от англ. console + append, «добавить в консоль») — выводит записи в консоль.
-  FileAppender (от англ. file + append, «добавить в файл») — сохраняет записи в файл.
-  RollerFileAppender (от англ. roll + file + append, «добавить в файл») — делает всё то же самое, что и FileAppender,
   но умеет автоматически создавать новый файл, если предыдущий заполнился записями до предела. Предел задаётся самим программистом.
   Например, когда размер файла log.txtпревысит установленный лимит в 10Мб, RollerFileAppender создаст новый файл log1.txt и будет добавлять записи в него.
   А когда и этот файл достигнет 10Мб, создаст log2.txt и переключится на него, и так далее.

                                                          Layouts

  Интерфейс Layout ответственен за конвертирование информации, получаемой от логера, в строку необходимого формата.
Вы можете создавать свои реализации типа Layout или использовать готовые классы, которые есть в Logback.
В абсолютном большинстве случаев программисты используют реализацию по умолчанию PatternLayout.
Этот класс позволяет очень гибко настраивать формат строки для вывода записи.
Например, можно задать такой шаблон вывода:

   %d{HH:mm:ss}: %message%n

   Рассмотрим элементы этого шаблона:

       %d указывает, что в этом месте будут выведены дата и время. Далее в фигурных скобках уточняется формат. В примере это {HH:mm:ss}.
       %message — в этом месте будет находиться текст самого сообщения.
       %n — перенос строки.

   В результате лог будет отформатирован в таком виде:


   14:06:49: Событие 1
   14:27:53: Событие 2
   ...
